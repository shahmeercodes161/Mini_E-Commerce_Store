{
  "name": "mini-ecommerce-store",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "npm --prefix Frontened install",
    "build": "npm --prefix Frontened install && npm --prefix Frontened run build",
    "start": "npm --prefix Frontened run preview"
  }
}